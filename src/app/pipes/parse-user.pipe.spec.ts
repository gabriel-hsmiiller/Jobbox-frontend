import { ParseUserPipe } from './parse-user.pipe';

describe('ParseUserPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseUserPipe();
    expect(pipe).toBeTruthy();
  });
});
